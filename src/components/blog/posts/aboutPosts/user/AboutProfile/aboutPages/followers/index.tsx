import { Empty } from "antd";

const Follow = () => {
//   let { user } = useParams();
//   let { data, isLoading, isError } = useQueryHandler({
//     pathname: "userhg",
//     url: `user/by_id/${user}`,
//   });
//   data?.followers.forEach((value: any) => {
//     let {data: folower,isLoading,isError,} = useQueryHandler({
//       pathname: "userhg",
//       url: `user/by_id/${value}`,
//     });
//     console.log(folower);
//   });

  return (
    <div>
      <Empty className="mx-auto my-10" />
    </div>
  );
};

export default Follow;
